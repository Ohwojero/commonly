import Link from 'next/link'
import { SiteChrome } from '@/components/site-header'

const steps = [{ number: '01', title: 'We look closely', text: 'We research products beyond the headline: how they are made, how they work, and whether they earn their place.' }, { number: '02', title: 'We make the edit', text: 'The collection stays intentionally small, with useful context and clear reasons for every recommendation.' }, { number: '03', title: 'You choose confidently', text: 'Read the details, compare the options, and follow the link to the retailer when something feels right.' }]

export default function HowItWorksPage() {
  return <SiteChrome><main className="min-h-screen bg-background pt-28"><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8 lg:py-24"><p className="text-xs font-semibold uppercase tracking-[0.25em] text-accent">The Commonly method</p><h1 className="mt-5 max-w-4xl font-serif text-5xl leading-[1.02] tracking-tight text-pretty sm:text-7xl">A better path from curious to considered.</h1><p className="mt-8 max-w-2xl text-lg leading-8 text-muted-foreground">We turn the overwhelming world of products into a calm, useful edit you can trust.</p><div className="mt-16 grid gap-px border border-border bg-border md:grid-cols-3">{steps.map((step) => <article key={step.number} className="bg-background p-7 sm:p-10"><p className="font-serif text-4xl text-accent">{step.number}</p><h2 className="mt-16 text-xl font-semibold">{step.title}</h2><p className="mt-4 leading-7 text-muted-foreground">{step.text}</p></article>)}</div></section><section className="bg-primary text-primary-foreground"><div className="mx-auto flex max-w-7xl flex-col gap-8 px-5 py-16 sm:flex-row sm:items-end sm:justify-between lg:px-8"><div><p className="text-xs uppercase tracking-[0.2em] text-primary-foreground/60">The short version</p><h2 className="mt-3 max-w-xl font-serif text-4xl leading-tight">Good products. Useful context. No unnecessary noise.</h2></div><Link href="/#shop" className="shrink-0 text-sm underline underline-offset-4">See the current edit</Link></div></section></main></SiteChrome>
}
